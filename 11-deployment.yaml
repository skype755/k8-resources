apiVersion: app/v1
kind: Deployment
metadata:
  name: nginx
  # lables of deployment resource
  labels:
    app: nginx
    tier: frontend
    purpose: deployment
  spec:
    replicas: 2
    selector:
      matchLabels:
        app: nginx  
        tier: frontend
        purpose: deployment
        namespace: expense
    template:
      metadata:
        labels:
          app: nginx
          tier: frontend
          purpose: deployment
      spec:
        containers:
        - name: nginx
          image: nginx:1.14.2
          ports:
          - containerPort: 80